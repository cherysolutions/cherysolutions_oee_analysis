version: 2

sources:
  - name: raw
    database: OEE_DEMO
    schema: RAW
    tables:
      - name: production_events_raw
        loaded_at_field: load_ts
        freshness:
          warn_after:
            count: 24
            period: hour
          error_after:
            count: 48
            period: hour
